<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>
  <body>
    <div class="container">
      <h2>Product Table</h2>
      <p><a href="/addproduct" class="btn btn-success">Add Product</a></p>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>ID</th>
            <th>Product Name</th>
            <th>Price</th>
            <th>size</th>
            <th>Details</th>
            <th>Product Image</th>
            <th colspan="2">Action</th>
          </tr>
        </thead>
        <tbody>
          <% let count= 0; if(ProductData.length > 0){%> <% for(let std of
          ProductData) { if (std.status != 0){%>
          <tr>
            <td><%=std.id%></td>
            <td><%=std.name%></td>
            <td><%=std.price%></td> 
            <td><%=std.size%></td>
            <td><%=std.details%></td>
            <td><img src="<%=std.image%>" alt="image" width="50px" height="50px"></td>
            <td><a href="/edit/<%=std._id%>" class="btn btn-primary">Edit</a></td>
            <td><a href="/delete/<%=std._id%>" class="btn btn-danger">Delete</a></td>
          </tr>
            <%}}}else{%>
          <td rowspan="7"><h3>No data Found</h3></td>
          <%}%>
        </tbody>
      </table>
    </div>
  </body>
</html>

